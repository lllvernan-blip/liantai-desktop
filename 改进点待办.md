# 改进点待办（阿楠 2026-09-18 口述）

> 三条已于 2026-09-18 当天实现并随 v0.0.6 发布。留档备查。

## 1. 划线句子在作答区下方显示 ✅ 已做（v0.0.6）
- 实现：作答区下方新增「我划的重点」面板，把字符级标记还原成原文片段随写随看；划/取消划实时同步。

## 2. 作答时也能做笔记（与学习卡笔记分开，数据里打通）✅ 已做（v0.0.6）
- 实现：作答页新增「本题笔记」（state.cache.qnotes，按题目签名存），旁边只读展示学习卡笔记，两本账互不覆盖、一起能看。

## 3. 完整参考答案 ✅ 已做（v0.0.6）
- 实现：批改返回新增 modelAnswer 字段（成文范文，不参与计分），结果页在采分点对照后以「参考答案（范文，仅供对照）」展示；老记录无此字段则不显示。计分口径一字未动。

## 1. 划线句子在作答区下方显示
- 现状：划线在上方材料里，写着写着要翻回前面看。
- 想要：已划的句子同步显示在作答区下方，作答时随看随写，不用来回翻。

## 2. 作答时也能做笔记（与学习卡笔记分开，数据里打通）
- 现状：只有学习卡阶段有笔记（`getNote(module, subtype)` 按「模块+子类型」存）。
- 想要：做题/作答阶段也能记笔记；与学习卡笔记是**两个东西**，但在数据层面结合（比如按题目/主题关联，复习时能一起看到）。
- 注意：存储 key 的粒度要想清楚——学习卡笔记按模块+子类型，作答笔记按题目签名（参考 `gw_marks` 按「题目签名」的思路）。

## 3. 完整参考答案
- 现状：批改后只有采分点对照（踩分点逐条比对）。
- 想要：再给一篇**完整参考答案**（成文的那种），可以对照着看自己差在结构还是表达。
- 注意：这会改 AI 出题/批改的返回结构（新字段），顺带要把断言和题型规范文档对齐。
